services:
  backend:
    build:
      context:
        ./backend
      dockerfile: 
        Dockerfile.prod

  frontend:
    build:
      context:
        ./frontend
      dockerfile: 
        Dockerfile.prod
      args:
        VITE_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
    ports:
      - "8000:80"
